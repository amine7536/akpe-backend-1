replicaCount: 1

image:
  repository: ghcr.io/amine7536/backend-1
  tag: latest
  pullPolicy: Always

service:
  port: 8080

ingress:
  enabled: true
  host: backend-1.127.0.0.1.nip.io

env:
  DATABASE_URL: "postgres://backend1:backend1@postgres-postgresql.main.svc.cluster.local:5432/backend1?sslmode=disable"
